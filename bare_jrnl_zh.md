# 从密码保护到状态与角色感知保障：面向未来卫星运行的安全防护

作者：Linan Huang、Houdong Wang、Linling Kuang

## 摘要

XXXX

## 1. 引言

未来卫星系统正由远程指令控制的通信终端，演进为网络化、多功能且自主性日益增强的计算平台 [kodheli2020satellite]。随着信息处理、计算与决策能力不断从地面迁移到星载系统，攻击面也从通信数据扩展到星上状态、处理逻辑和控制流程 [thangavel2024artificial]。

卫星通信系统已在多个协议层形成广泛的密码保护机制。代表性方案包括面向传统空间链路的 CCSDS 空间数据链路安全协议 [ccsds2022sdls]，以及可适配新兴 IP 化空天地一体化网络的互联网安全协议（如 IPsec）[ahmad2022security]。这些机制通过认证消息来源、保护消息完整性和维持机密性来确立密码学有效性。

然而，密码学有效性并不保证运行合法性。一条通过正确认证且完整性受到保护的消息，在当前运行情境下仍可能不应被接受。例如，经由受陷用户终端注入的一条通过正确认证的遥测、跟踪与指令（TT&C）消息，如果该终端被分配的角色不允许执行航天器控制操作，就仍应被拒绝。

因此，未来卫星安全必须在传统密码保护之外加入情境感知保障：依据当前节点角色、系统状态、任务阶段和运行上下文，判断一项已认证操作是否具有运行合法性，进而保障星上运行的正确性、连续性与韧性。

弥合这一缺口带来三项根本挑战。第一项挑战是协调状态复杂性与可信性。未来卫星需要维护丰富且不断演化的安全状态，以支持明文/保护模式管理与切换等具有韧性的安全机制 [huang2026toward]。然而，状态越复杂，管理逻辑越难验证，攻击面也越大。关键问题因而是：如何在支持复杂安全状态演化的同时，使安全关键的状态管理核心保持可信并能抵御攻击。

第二项挑战涉及 XXXX。

第三项挑战涉及对运行合法性的系统级验证。随着卫星系统日益网络化、分布式化和自主化 [thangavel2024artificial]，与安全相关的证据会分散在不同节点和运行阶段，无法始终依靠孤立信息作出可靠解释。这些证据可能异构、不完整，甚至相互矛盾。因此，关键挑战是设计能够主动引出、协调并交叉验证分布式证据的机制，以支持可靠的安全决策，同时适应卫星特有架构和严格的尺寸、重量与功耗（SWaP）约束。

本文旨在弥合上述缺口。针对第一项挑战，我们提出一种最小化解耦架构，通过明确的控制与数据接口，将复杂且任务特定的状态管理与最小化、标准化的密码单元分离。在此基础上，我们界定两个单元的职责，并建立状态评估与密码执行的规范处理顺序，从而保证多个相互依赖安全状态得到一致处理。

针对第二项 XXXX 挑战，我们 XXXX。

针对第三项挑战，我们采用角色感知策略。我们不让每个节点承担同等的安全义务，也不期望单一节点独自完成对操作的全面验证，而是在源、转发、目的和执行节点之间分配差异化职责。职责分配依据各节点在工作流中的位置、可获得的上下文以及实际验证能力进行定制。

如图 1 所示，本文通过在第 2、3、4 节分别解决三项挑战，构建从传统密码学有效性扩展至情境感知安全的整体结构。第一项工作可视为将传统密码处理扩展到复杂系统状态，从而奠定可信基础；第二、第三项工作则分别从可感知性和可验证性的角度进一步迈向情境感知安全。

![图1：XXXX](figure/PaperStructureV2.pdf)

## 2. 复杂安全状态管理下的可信密码处理

### 2.1 安全状态复杂性与设计原则

星载安全状态管理的复杂性正沿三个紧密耦合的维度上升。第一，现代卫星同时承载具有不同关键程度和保护需求的异构业务流，因此逐流安全管理比整条链路采用单一配置更为合适。遥控指令等任务关键流还可同时支持保护模式和明文模式 [huang2026toward]，以便在星载密码功能失效时维持基本功能。

第二，每条流都与相应的安全关联（SA）相联系，而不同 SA 可以引用不同密钥，也可以共享同一密钥。这形成了复杂的“流—SA—密钥”依赖和映射关系，其中一个安全对象的变化可能影响多条流或多个处理上下文。

第三，密钥管理正从静态预装密钥池演进为支持空中密钥更新。密钥的生成、激活、轮换、过期和撤销引入了时间维度上的复杂性；同时，由于星上与地面必须在丢帧、传输错误、确认延迟和间歇连接条件下保持对活动密钥的一致认知，它还引入了分布式状态一致性要求。

因此，系统复杂性越来越多地并非源自密码算法本身，而是源自决定何时、何地以及如何调用密码操作的演化安全状态。这种扩展的状态管理逻辑也扩大了攻击面：攻击者可能破坏某条流的明文/保护状态，操纵流到密钥的映射，或扰乱星地密钥同步，致使合法流量被拒绝或未授权操作被接受。由此得到以下两项设计原则。

**原则 1（保持密码核心精简）**：安全状态管理应与密码执行解耦，仅将依赖密钥的功能保留在最小化且受到严格控制的密码边界内。

**原则 2（低成本检查优先，尽早拒绝）**：安全检查应从简单且不依赖密钥的检查逐步推进至高成本密码操作，从而尽早拒绝无效请求并降低拒绝服务风险。

以下两节基于这些原则介绍解耦架构和渐进式安全处理顺序。

### 2.2 具有受控接口的解耦架构

CCSDS [ccsds2020sdlsep] 通过彼此分离但相互协调的生命周期来管理 SA 与密码密钥，为第一项原则提供了成熟先例。SA 依次经历 *Null、Unkeyed、Keyed、Operational* 状态，而密钥则独立经历 *Pre-Activation、Active、Deactivated、Destroyed* 状态。二者通过 *Rekey SA* 流程建立关联。

在这种成熟分离方式的基础上，我们将受管理的状态空间扩展到 SA 和密钥生命周期之外。更广的状态空间要求在运行状态管理与密码执行之间重新划定可信边界。如图 1 底部所示，所提架构由最小可信密码（MTC）单元与运行安全状态管理和决策（OSMD）单元组成。MTC 仅负责受保护的密钥材料存储和密码算法执行，其可执行逻辑在发射前固定，入轨后不可重配置。OSMD 则维护全部运行安全状态，包括逐流安全模式状态、逐 SA 抗重放状态，以及 SA 和密钥的生命周期状态。它还负责安全处理决策，其可重配置逻辑可在轨更新，以适应不断变化的运行需求和攻击策略。

所有外部请求均终止于 OSMD，从而使 MTC 不直接暴露给其他星载或地面实体。OSMD 接收星地消息，依据当前运行安全状态进行验证，并结合卫星特定约束决定处理方式。其处理顺序尤其考虑有限的 SWaP 资源及由此产生的拒绝服务攻击易感性。

OSMD 与 MTC 通过两个狭义定义的内部接口交互。第一个是控制接口，仅支持直接涉及密码密钥材料的操作。在对称密钥实现中，运行密钥由 MTC 使用预装根密钥派生，不再需要时在 MTC 内安全删除。激活、停用等其他密钥生命周期状态和决策均留在 OSMD 内，MTC 不感知这些状态。例如，停用密钥只是在 OSMD 中将其标记为后续不可使用；销毁密钥则还会触发 MTC 中的安全删除。因此，控制接口仅传递诸如 $(\mathit{ParentKeyID},\mathit{Derive},\mathit{DerivedKeyID},\mathit{Parameters})$ 和 $(\mathit{KeyID},\mathit{Delete})$ 的简单命令，并且只返回执行状态；秘密密钥材料不会跨越该接口传输。

第二个是逐消息数据接口，OSMD 通过该接口请求 MTC 执行密码操作。每项请求包含原始消息、所请求的密码服务、选定的密钥标识符以及操作特定参数。MTC 仅返回处理后的消息和执行状态。这样，接口将密码执行作为狭义服务暴露，同时把全部状态解释和安全决策保留在 OSMD 内。

上述接口为所提架构给出了具体的对称密钥实现。通过增加少量控制操作，例如密钥对生成和共享密钥建立，也可支持非对称密码。生成的私钥和共享秘密仍保留在 MTC 内，只有公钥和执行状态返回 OSMD。因此，支持非对称密码不会改变解耦架构或最小接口原则，只会扩展受控接口所暴露的密码操作集合。

### 2.3 依赖感知的安全处理

与原则 2 一致，图 2 展示了跨 OSMD 与 MTC 的渐进处理顺序。OSMD 首先执行轻量、不依赖密钥的检查，只有在所有适用的密码处理前检查均成功后才调用 MTC。持久安全状态只能依据通过密码认证的输入进行修改。该顺序可尽早拒绝无效帧、节省稀缺的星载密码资源，并防止未经认证的输入破坏持久安全状态。

![图2：OSMD 与 MTC 之间依赖感知的渐进处理。外、内红色虚线边界分别表示二者的处理范围。](figure/Point1V1.pdf)

处理始于逐流保护状态验证。OSMD 为每条已识别数据流维护权威的“系统侧流状态”（SFS），规定该流当前应以明文模式还是保护模式运行。每个接收帧通过实际保护格式呈现相应的“接收流状态”（RFS）。只有 RFS 与 SFS 一致时 OSMD 才继续处理；否则，在进行任何 SA 处理或密码操作之前即拒绝该帧。

对于受保护流，每个接收帧携带“接收 SA 标识符”（RSAI），表明其声称采用的 SA。RSAI 只被视为不可信引用，用于定位由 OSMD 权威维护的相应“本地 SA 上下文”（LSAC）。LSAC 决定该 SA 及相关密码资源是否仍获准使用。例如，RSAI 可能指向现有 SA，但按本地生命周期状态，与该 SA 关联的密钥已经过期。此时 OSMD 无需调用 MTC 即可拒绝该帧。因此，RSAI 标识输入帧所声称的 SA，而 LSAC 决定该 SA 在本地是否仍有效且在运行上可用。

RSAI 解析到现有 LSAC 后，OSMD 执行轻量的抗重放预检查。我们区分从输入帧提取的“接收序列号”（RSN）与“本地重放状态”（LRS）；后者是引用 SA 的本地管理序列号 [ccsds2022sdls]。

CCSDS SDLS 提供了基准抗重放机制。接收方先验证受保护帧的完整性，再将已认证 RSN 与相应 LRS 比较；只有两项检查均成功后才更新 LRS [ccsds2022sdls]。这种顺序可防止未经认证的 RSN 修改持久重放状态，否则攻击者可注入足够大的序列号，使后续合法帧被拒绝并造成持续拒绝服务。但基准顺序也意味着包括明显重放帧在内的每个接收帧都要消耗密码处理时间和能量，这对 SWaP 约束严格的星载平台尤其不利。

为降低开销，我们借鉴 IPsec ESP [kent2005security4303] 引入两阶段重放检查。密码验证前，OSMD 将未经认证的 RSN 与当前 LRS 比较。明显陈旧或超出配置允许前进范围的帧无需调用 MTC 即被拒绝。由于 RSN 尚未认证，此预检查仅作早期过滤，不能修改 LRS。

通过重放预检查的帧随后接受完整 LSAC 验证。只有关联到有效且获授权 SA 上下文的帧才会送往 MTC 进行密码处理。密码认证成功后，OSMD 使用已认证 RSN 与当前 LRS 执行最终重放检查。只有该检查成功，才更新 LRS 以纳入 RSN。因此，预检查减少不必要的密码操作，而认证后的后检查仍是提交重放状态转换的唯一权威依据。

总体而言，每个处理阶段仅依赖前序阶段已经确立的信息。密码执行前，可接受帧集合逐步收窄；持久状态转换则推迟至相应输入通过认证之后。

## 3. 受约束的安全状态感知与重构

第 2 节为复杂安全状态的星上管理与应用建立了可信基础。剩余问题是：当这些状态无法完整导出时，地面能够可靠推断出什么。现有机制提供了有用但彼此割裂的观测：CCSDS SDLS 及其扩展流程负责管理 SA 以及相关监测与控制功能 [ccsds2022sdls, ccsds2020sdlsep]；RATS 则提供通用架构，用于生成和评估有关证明方运行状态的证据 [rfc9334rats]。然而，单独有效的报告并不能证明相关对象在一段时间内共同构成了端到端业务流预期的保护上下文。

设想一条受保护流迁移到新的卫星路径。新路径上的平台健康且 SA 处于活动状态，并不能证明新保护上下文在流量迁移前已经就绪，也不能证明旧上下文一直有效至迁移完成。因此，目标不是复制一份完整的敏感星载状态，而是重构足以判断选定流级安全属性的证据。这里存在两项约束：一部分状态值和跨层关系必须留在星上可信边界内；间歇性回传链路又无法实时承载所有允许披露的证据。我们分三步处理：定义所需的安全语义，将其编码为披露安全的证据，再根据证据对地面判断的贡献渐进传输。

### 3.1 以流为中心的安全状态语义

现有 SA 管理、平台证明、路由和策略功能描述的是单个对象或子系统，而流安全取决于这些对象之间的关系。因此，我们使用持久业务流标识，将每个参与节点的五类状态关联起来：平台与启动实例、保护模式与 SA 绑定、密钥纪元、策略版本和路由纪元。当路径、SA 或密钥发生改变时，该标识仍保持稳定，使密钥更新、路径迁移和复位恢复都能关联到同一业务上下文。

四类属性用于刻画该上下文是否持续安全：*状态有效性*检查每个被引用对象是否仍可使用；*绑定一致性*检查流、SA、密钥、策略和路由纪元是否属于同一保护上下文；*时间连续性*要求整个服务区间始终受到保护；*转换安全性*约束状态变化顺序。对一次路径迁移，有

$$
t_{\mathrm{new\ protection}} \leq t_{\mathrm{migration}} \leq t_{\mathrm{old\ withdrawal}}.
\tag{1}
$$

新上下文必须在承载流量前就绪，旧上下文必须保持可用直至迁移完成。本阶段输出一个属性依赖模型，规定判断每项流级属性所需的状态、关系、边界事件和上下文。

### 3.2 披露安全的证据与可靠重构

RATS 确立了证据生成与评估角色，并明确承认证据可能包含敏感信息 [rfc9334rats]；受限披露证明进一步表明，无需发布完整测量日志也可以证明选定属性 [eckel2023racd]。这些机制提供了基础，却没有定义卫星保护转换所需的流级、跨对象和时间性声明。

因此，我们导出经过认证的属性声明，而非原始状态。卫星无需披露新 SA 标识符及其精确激活时刻，而可断言：“与该流及路由纪元相关的保护上下文在流量迁移前已经投入运行。”该声明保留判断式（1）所需的顺序关系，同时隐藏实现特定标识符，并在需要时隐藏精确事件时刻。每项声明都绑定受控流标识、报告节点或角色、启动与路由上下文、有效区间、所声明属性、来源和新鲜度。OSMD 依据权威运行状态评估属性，MTC 则保护声明认证密钥，从而维持第 2 节所述的可信边界分离。

证据表示可以适应声明的敏感程度：可报告属性可使用签名断言，稳定区间可使用认证摘要，敏感成员关系或关联可使用承诺或零知识证明。因此，本文关注声明语义，而不是提出新的披露原语。由于披露会主动移除部分信息，地面不会强行构造唯一的内部状态历史，而是保留所有与依赖模型及已接收声明一致的演化。仅当所有剩余演化均满足某属性时，该属性才为“已验证”（Verified）；仅当所有演化均违反该属性时，才为“已违反”（Violated）；否则结论为“不确定”（Indeterminate）。相互矛盾的声明则表明证据或模型异常。这一保守规则与部分信息下的运行时验证一致 [leucker2019partial]。

### 3.3 资源感知的渐进重构

最后一步在不放宽披露边界的前提下加入回传链路稀缺性。Bundle Protocol Version 7 支持延迟与中断容忍传输 [rfc9171bpv7]，CCSDS SDLS 扩展流程则提供标准化的监测与控制服务 [ccsds2020sdlsep]。这些机制规定信息如何传递或报告，但如何依据证据对安全结论的贡献进行应用特定选择，仍是一个独立问题。

我们按照判断价值而非通用数据优先级，对允许披露的证据进行排序。能够确立违规、弥合尚未解决的属性缺口、覆盖关键转换或即将失去新鲜度的证据优先；对于价值近似的证据，再以传输大小、生成成本、能耗和截止期限加以区分。稳定区间可以压缩成认证摘要，关键转换则保留为可独立验证的声明。每次接触后，地面验证声明来源、上下文、区间、新鲜度和模型版本，并排除不一致的状态演化。若结论仍为“不确定”，地面只针对相关流、节点或角色、纪元和区间请求尚未解决的属性。此类请求不得突破前述披露边界。

在路径迁移示例中，如果首次接触报告了迁移和随后对旧上下文的撤销，却遗漏了新路径就绪信息，那么安全转换与无保护间隙都仍有可能。因此，地面只请求该就绪属性。之后的声明要么确立式（1），要么表明迁移发生在就绪之前。资源稀缺可以延迟结论，却不能降低“已验证”或“已违反”的判定门槛。最终记录——流、属性、结论、上下文和有效区间——为地面提供边界明确的情境视图，作为第 4 节实时分布式验证的补充。

## 4. 角色感知的运行合法性验证

前两节分别建立了复杂安全状态管理下的可信密码基础，并使地面段能够可靠感知星载安全状态。在此基础上，本节通过主动生成并协调全系统证据的系统级验证，进一步迈向运行合法性保障。

在具备星上路由和处理能力的未来网络化卫星系统中，一项安全关键操作在最终执行前可能经过多个节点和处理阶段。如图 3 所示，我们将该过程抽象为源、转发和目的三类节点角色。由于这些角色可获得的信息和可采取的防御动作不同，以下三节分别提出与其能力相适配、彼此互补的验证机制。

![图3：沿消息处理链，在源、转发和目的节点之间进行角色感知的分布式运行合法性验证。绿色、蓝色和紫色模块分别表示端点间动作—效果一致性验证、转发节点路径可接受性验证，以及目的节点执行一致性验证。](figure/Point3V3.pdf)

### 4.1 基于语义冗余的端到端验证

第一种机制源自如下认识：当目的节点正确执行指令时，源节点能够确定指令的预期效果。

如图 3 绿色模块中的虚线所示，传统指令效果端到端验证主要以地面为中心且属于事后验证，通常依赖星上执行后返回的遥测 [ecss7031c]。目的节点先执行上行帧携带的动作，再通过遥测报告由此产生的状态变化。因此，源节点只有在收到相应遥测后才能完成验证；当受限的星地带宽只能低频下传相关状态时，这可能带来显著延迟。

为此，我们在上行帧中加入预期效果字段（EEF），作为执行前验证的“语义校验和”。源节点根据预期动作及其本地维护状态推导 EEF，并嵌入帧中。目的节点收到后，使用接收动作和自身当前状态独立地重新计算 EEF。只有两个 EEF 值相符时才执行动作。

通过引入语义冗余，EEF 能够在星上动作执行前进行实时端到端验证。它可以检测两类不一致：星上状态发生非预期变化，以及动作编码错误，从而防止非预期执行。

**示例（功率等级调整）**：设星载发射机具有离散功率等级，并有两种动作可令当前等级增加或降低一级。若源节点认为当前等级为 $k$ 并发出增加动作，它将 EEF 设为 $k+1$。目的节点根据接收动作和实际状态独立推导预期动作后等级，仅当结果与 EEF 相符时才执行。若不相符，则说明星上状态发生了非预期变化（如恶意篡改或重启导致的复位），或者动作编码错误（如把预期的增加编码成降低）。无论哪种情况，动作均在执行前被拒绝。

### 4.2 轻量转发节点准入控制

转发节点通常只检查转发所需的首部字段。考虑其 SWaP 约束和多流聚合流量巨大，在每个转发节点进行载荷检查或部署完整 IDS 既不现实，也与目的端检测高度重复。

如图 3 蓝色模块所示，我们改为使用紧凑且完整性受保护的首部元数据，使转发节点执行轻量准入控制；元数据标识源、目的和消息类型。每个转发节点将其与本地路径上下文结合，判断流量从自身所在转发位置看来是否可以接受。

以引言中的攻击为例：受陷用户终端注入格式正确且在密码学上有效的 TT&C 消息。第一跳星载路由器发现消息来自用户接入域且被归类为 TT&C 流量。由于该域无权产生此类流量，路由器在转发前拒绝该帧。

这种早期过滤很重要，因为入口来源及其他路径特定上下文可能在转发后丢失或变得模糊。下游节点若要检测同一违规行为，可能必须进行成本高得多的载荷检查或入侵检测。只需增加少量首部元数据，便可在不可接受流量的入口附近以低成本抑制它。

现有机制为该设计提供了部分基础。IEEE 802.1Qci [ieee8021qci] 通过逐流过滤与监管（PSFP）建立了轻量转发面执行的工程基线；本文机制则将其从流量合规性扩展到运行可接受性。RFC 3704 [rfc3704] 规定入口过滤和单播反向路径转发（uRPF），用于评估分组到达方向是否与其声称的源地址一致。本文机制超越了源的合理性，还进一步验证所声称的源是否有权产生所声称的消息类型。

### 4.3 基于输出的异构执行验证

在最终执行操作的目的节点，可有意引入额外计算资源，以增强对安全关键操作的保障。

第 4.1 节的端到端机制主要验证源与目的节点所维护*状态*的一致性，而此处机制关注*执行输出*的一致性。在复杂系统中直接比较全部内部状态通常不可行，因为相关状态可能数量众多、依赖具体实现或具有安全敏感性。尤其是密码密钥或密钥相关标识符等状态，应局限在各自硬件或可信处理边界内。因此，我们通过外部可观察输出验证执行一致性，而不直接暴露或比较内部状态。

如图 3 紫色模块所示，主处理模块配有一个或多个异构影子模块。输入消息先经过消息认证模块，执行第 2 节的密码检查和第 4.1 节的端到端语义验证。只有通过这些检查的消息才会同时分发给主处理模块和影子模块。各模块独立处理同一已认证输入，其输出被送往输出验证模块。只有结果满足预定义一致性规则时才接受该操作；否则阻止或推迟执行，或者报告异常。

异构实现有助于缓解共模故障和共模入侵。虽然冗余处理单元广泛应用于卫星系统，但同构副本可能共享相同漏洞、缺陷或依赖，使单一故障或攻击产生一致但错误的输出。因此，我们采用不同 CPU 架构、软件实现或处理范式，例如组合基于 CPU 和 FPGA 的模块，使输出一致能够为正确执行提供更强证据。

为适应星载资源约束，异构冗余只对安全关键操作选择性激活。主模块产生待提交输出，影子模块生成候选输出而不直接影响受控系统。可依据操作关键程度、可容忍时延、估计攻击概率和可用资源，在星上自主调整活动影子模块数量；此类自适应调度机制的设计留待未来工作。

## 5. 综合案例研究与评估

本节把前述机制整合到端到端案例研究中，并评估其在代表性攻击场景下的有效性。

### 5.1 攻击场景

如图 4 所示，我们考虑四类代表性攻击场景，涉及受陷卫星终端、TT&C 站、TT&C 中心以及星载处理模块。

在第一种攻击场景（A1）中，攻击者攻陷分布于全球的合法卫星终端（ST），从而获得卫星网络接入能力，并通过用户链路发送数据帧。假定攻击者了解系统协议，但不具备生成有效 TT&C 指令所需的凭据和能力。

在第二种攻击场景中，攻击者攻陷 TT&C 站并获得 TT&C 上行链路接入能力。通过观察合法传输，攻击者可以获知明文模式帧格式，并记录此前发送的密文模式帧，从而分别实施明文模式帧注入（A2a）和密文模式帧重放（A2b）。

在第三种攻击场景（A3）中，攻击者部分攻陷 TT&C 中心并获得生成伪造指令的能力。在第四种攻击场景（A4）中，攻击者攻陷星载处理模块并操纵其执行或输出。

![图4：具有星上路由和处理能力的卫星星座所面临的代表性攻击。A1 表示由受陷卫星终端发起的 TT&C DDoS；A2a、A2b 分别表示来自受陷 TT&C 站的明文模式帧注入和密文模式帧重放；A3 表示来自受陷 TT&C 中心的指令伪造；A4 表示星载处理模块受陷。绿色实线为合法处理路径，红色虚线为攻击路径。](figure/CaseStudyV3.pdf)

### 5.2 基线与攻击流量的仿真设置

设合法 TT&C 上行基线速率为 $R_{\mathrm{L}}=1\,\mathrm{Mb/s}$，并叠加以下攻击流量。攻击负载根据攻击者接入能力和相应通信链路容量进行参数化。

对于 A1，假设攻击者利用共同漏洞攻陷 $K=100$ 个卫星终端。每个受陷终端的最大发送速率为 $R_{\mathrm{A1}}=20\,\mathrm{Mb/s}$。为表示不同攻击强度，每个终端以 $\lambda R_{\mathrm{A1}}$ 的速率生成恶意流量，其中 $\lambda\in[0,1]$ 为归一化攻击强度参数。因此，A1 聚合流量速率为 $K\lambda R_{\mathrm{A1}}$，范围为 $0$ 至 $2\,\mathrm{Gb/s}$。

对于 A2，考虑一个受陷 TT&C 站，以 $R_{\mathrm{A2}}=5\,\mathrm{Mb/s}$ 的速率实施明文模式帧注入（A2a）或密文模式帧重放（A2b）。

对于 A3，受陷 TT&C 中心以 $R_{\mathrm{A3}}=0.1\,\mathrm{Mb/s}$ 的速率生成伪造指令。只有当 A3 与 A4 协同时，即执行单元的主模块已被攻陷并产生错误输出时，这些伪造指令才会导致错误系统效果。

### 5.3 资源受限处理模型

星载处理链由四个阶段组成：路由器、OSMD、MTC 和执行单元。尽管这些阶段承担不同安全功能，其有限处理资源均使用统一的阈值流体模型表示。

对于输入流量速率为 $r_j$、标称处理能力为 $C_j$ 的处理阶段 $j$，当 $r_j\le C_j$ 时，全部输入帧都能接受相应处理；当 $r_j>C_j$ 时，只有输入帧中的 $C_j/r_j$ 比例能够得到处理。因此，输入帧被选中处理的概率建模为

$$p_j^{\mathrm{sel}}=\min(1,C_j/r_j).$$

该抽象遵循容量受限流体建模的一般思想 [misra2000fluid]。未选中部分 $1-p_j^{\mathrm{sel}}$ 的处理方式则取决于各阶段的安全角色。

在路由器和 OSMD 处，被选中的帧接受概率安全检查。路由器应用角色感知准入控制检测 A1 流量，OSMD 则执行状态感知和重放感知验证检测 A2 流量；被识别为恶意的帧会被拒绝。未选中检查的帧绕过当前阶段并转发到下一阶段。因此，检查能力不足会降低检测覆盖率，但不会直接造成丢帧。

MTC 是密码学有效性的最终强制关口。其处理能力范围内选中的帧接受密码验证，无法处理的帧则直接丢弃，而不会在未经验证的情况下继续转发。因此，当 $r_{\mathrm{MTC}}>C_{\mathrm{MTC}}$ 时，资源导致的丢帧概率为

$$p_{\mathrm{MTC}}^{\mathrm{drop}}=1-C_{\mathrm{MTC}}/r_{\mathrm{MTC}}.$$

这种故障关闭行为可以防止未经密码验证的帧到达执行单元，但在严重过载时也可能丢弃合法帧。

主执行模块的处理能力与 MTC 的最大输出速率相匹配，因此 MTC 接受的所有帧都能由主模块实时处理。此外，还会激活异构影子模块来检测协同的 A3—A4 攻击。由于影子执行会产生额外处理开销，当输入速率超过可用影子处理能力时，只有部分已接受帧能够获得影子验证。

令 $C_{\mathrm{shadow}}$ 表示异构影子执行的可用能力，$r_{\mathrm{exec}}$ 表示 MTC 接受的流量速率。帧被选中进行影子验证的概率建模为

$$p_{\mathrm{shadow}}=\min(1,C_{\mathrm{shadow}}/r_{\mathrm{exec}}).$$

当 $r_{\mathrm{exec}}\le C_{\mathrm{shadow}}$ 时，所有帧都接受影子执行，A3—A4 攻击以概率 1 被检测；当 $r_{\mathrm{exec}}>C_{\mathrm{shadow}}$ 时，只有部分帧接受影子执行。未选中影子验证的帧仍由主模块处理并获准继续。因此，有限的影子资源不会降低合法流量交付率，却可能让涉及受陷主模块的攻击逃过检测。

路由器、OSMD 和 MTC 的标称能力分别设为 $C_{\mathrm{router}}=1000\,\mathrm{Mb/s}$、$C_{\mathrm{OSMD}}=500\,\mathrm{Mb/s}$ 和 $C_{\mathrm{MTC}}=50\,\mathrm{Mb/s}$。主执行能力配置为支持 MTC 接受的全部流量；在相应实验中改变 $C_{\mathrm{shadow}}$，以评估影子验证覆盖率与处理开销之间的权衡。

### 5.4 仿真结果

（原稿此节为空。）

## 6. 结论

1. 一种由原则指导的解耦结构。
2. XXX
3. 一套系统化解决方案，使处于不同位置的每个组件都能发挥各自作用。

## 参考文献

参考文献条目沿用原稿的 `RSE.bib`，文中方括号内为对应 BibTeX 引用键。
